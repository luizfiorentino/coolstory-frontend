export const selectAllSpaces = (state) => state.space.allSpaces;

export const selectSpaceDetails = (state) => state.space.spaceDetails;

export const spaceDetails = (state) => state?.user?.userSpace;
